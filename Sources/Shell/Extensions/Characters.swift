public extension Character {
 static let asterik = Character("*")
 static let backslash = Character("\\")
 static let forwardslash = Character("/")
 static let comma = Character(",")
 static let startBrace = Character("{")
 static let endBrace = Character("}")
 static let questionMark = Character("?")
 static let startBracket = Character("[")
 static let endBracket = Character("]")
 static let exclamationMark = Character("!")
 static let caret = Character("^")
 static let hyphen = Character("-")
}

public extension String {
 static let asterik = String("*")
 static let backslash = String("\\")
 static let forwardslash = String("/")
 static let comma = String(",")
 static let startBrace = String("{")
 static let endBrace = String("}")
 static let questionMark = String("?")
 static let startBracket = String("[")
 static let endBracket = String("]")
 static let exclamationMark = String("!")
 static let caret = String("^")
 static let hyphen = String("-")
}
